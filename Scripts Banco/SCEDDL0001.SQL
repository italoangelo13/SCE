/*CREATE DA TABELA DE CONTAS A RECEBER*/
CREATE TABLE `u348631407_dhd6z`.`SCETBLCAR` (
  `CARCOD` INTEGER UNSIGNED NOT NULL AUTO_INCREMENT,
  `CARTITULO` VARCHAR(255) NOT NULL,
  `CARDESCRICAO` VARCHAR(500),
  `CARDATAVENCIMENTO` DATETIME NOT NULL,
  `CARDATACADASTRO` DATETIME NOT NULL,
  `CLICOD` INTEGER UNSIGNED,
  `PEDCOD` INTEGER UNSIGNED,
  `CARVALOR` DECIMAL(15,2) NOT NULL,
  `CARSTATUS` VARCHAR(1) NOT NULL DEFAULT 'P',
  PRIMARY KEY (`CARCOD`),
  CONSTRAINT `FKCAR0001` FOREIGN KEY `FKCAR0001` (`CLICOD`)
    REFERENCES `scetblcli` (`CLICOD`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `FKCAR0002` FOREIGN KEY `FKCAR0002` (`PEDCOD`)
    REFERENCES `scetblped` (`PEDCOD`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT
)
ENGINE = InnoDB
COMMENT = 'TABELA DE CONTAS A RECEBER';